小笼洛包 更新日志
============================

本文件按版本记录小笼洛包的更新内容，随源码仓库与发布包一并分发；
后续发布新版本时，请在本文档顶部继续追加对应版本的更新条目。

──────────
1.3 版本更新
──────────
1. 修复 WebUI 中 Ollama 状态显示不真实的问题：
   · 仅在实际使用 Ollama（标准模式）时才显示"Ollama 可用"及其模型列表；
   · Lite 模式（仅外部 API）下状态栏正确显示"外部API · 未配置"，不再误显本机 Ollama 模型名。
2. 优化界面状态栏显示：
   · 新增"模式"徽章（Lite（仅外部API）/ 标准）；
   · "模型"徽章按实际后端显示（Ollama / 外部API）。
3. 桌面版窗口组件按 Python 版本条件安装：
   · Python 3.13+ 自动跳过 pywebview（与 pythonnet 不兼容），桌面版改用 Edge/Chrome 应用模式窗口；
   · 使用 gpt_sovits 自带 Python（3.9）建 venv 时正常安装 pywebview，使用内嵌窗口。
4. 新增运行日志（runtime/logs，按大小自动轮转并设保留上限，不会无限增长），
   记录运行状态与报错信息。
5. 诊断工具新增桌面版窗口组件检查（含 Python 版本与 webview 兼容性提示）。
6. 版本号更新为 1.3。

──────────
1.1 版本更新
──────────
1. 项目定位统一为"智能聊天软件"，版本号更新为 1.1；
   界面与命令行文案去除表情符号，统一为纯文字提示。
2. 修复 GPT-SoVITS 启动问题：
   · 优先使用独立 GPU（CUDA）运行，无可用 GPU 时自动切换为 CPU 模式；
   · 模型权重路径改为包内相对路径，可在任意目录运行。
3. Ollama 改为可选加载：
   · 已安装则自动启动；未安装或启动失败时跳过加载，
     并提示"将只能通过外部 API 调用大模型"；
   · 服务就绪等待移至后台，不再阻塞 Web 界面启动。
4. 新增启动前关键组件预检：
   · 关键文件 / 依赖缺失时提示运行 setup\install.bat 并终止启动；
   · 不擅自安装或修改任何文件。
5. 修复安装程序在中文系统下的解析崩溃（编码与换行问题），
   新增对微软商店占位 Python 的识别；支持按模块手动确认、
   可反复运行补装缺失组件、全部安装完成时给出明确提示。
6. 安装程序全面使用国内镜像：
   · pip 使用清华源；Whisper 模型使用 hf-mirror；
   · Ollama 安装包使用 gh-proxy 加速下载。
7. Web 界面端口被占用时自动顺延，并在服务就绪后自动打开浏览器。

──────────
1.1 更新补充
──────────
1. 移除"更优的播放列表"插件（点歌播放、音乐队列功能不受影响）。
2. 修复 Web 界面偶发"无法访问"问题：
   · 默认端口调整为 10999，端口被占用时自动改用随机端口兜底；
   · 修复 Windows 下端口重复绑定问题，重复启动时自动复用已在运行的服务；
   · 新增环境自检工具（诊断.bat），一键定位安装 / 依赖 / 端口 / 服务问题。
3. 新增桌面版入口：启动时可选 桌面版（内嵌窗口）/ 网页版（浏览器），
   并可选择 Lite 模式（仅加载语音合成，大模型使用外部 API）或标准模式（启动全部服务）；
   桌面版等待服务就绪后打开窗口，始终指向实际可用地址。
4. 验证 GPT-SoVITS 纯 CPU 推理可行：无独立显卡时自动切换 CPU 模式
   （实测模型加载约 20 秒、单句合成约 16 秒），无 GPU 机器也可正常使用语音合成。

──────────
1.0 正式版
──────────
1. 一键安装的 Python 环境校验，修复可能的启动崩溃问题。
2. 取消 Ollama 强制安装，可在完全外部 API 模式下运行。
3. 暂时移除有问题的"更好的播放列表"插件。

*感谢 QQ 群 743918078 中群友对体验版的大力支持
*遇到任何问题欢迎入群反馈，群号：743918078
